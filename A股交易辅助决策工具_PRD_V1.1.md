# A 股交易辅助决策工具 PRD V1.1

## 版本管理

| 版本号 | 日期 | 变更类型 | 变更详情 |
|---|---|---|---|
| V1.0.0 | 2026-07-09 | 新建 | A 股交易辅助决策工具第一版 PRD |
| V1.1.0 | 2026-08-25 | 修订 | 补充数据契约、指标与信号口径、同步状态机、数据模型、非功能需求、验收标准和分阶段范围 |

## 修订决策

V1.1 按以下边界进入研发；后续若改变边界，需要同步更新数据契约和验收标准。

| 决策项 | V1.1 结论 |
|---|---|
| 使用场景 | 单用户、本机 Web 工具，仅监听 `127.0.0.1` |
| 数据频率 | 日线级，只使用最新已完成交易日数据，不提供盘中实时行情 |
| 数据源 | AkShare，通过独立数据源适配层接入；具体接口在开发前 Spike 中锁定 |
| 数据库 | SQLite；暂不引入 PostgreSQL |
| 价格口径 | 页面展示不复权行情；指标和信号使用前复权行情，成交量使用原始成交量 |
| 指标计算 | 同步完成后统一预计算并缓存，筛选、提醒、详情和报告不得重复实现公式 |
| 提醒能力 | P0 提供内置只读规则；P1 再提供规则增删改、参数配置和启停 |
| 筛选方案 | P0 提供默认候选方案和临时组合筛选；保存筛选条件为 P1 |
| 报告导出 | V1 只导出 Markdown；PDF 延后 |
| 决策记录 | 轻量关注笔记列为 P1，不阻塞 P0 主链路 |
| 对外边界 | 不自动交易、不预测股价、不输出买卖建议或收益承诺 |

## 一、背景和目标

### 1.1 需求背景

个人 A 股研究需要反复完成大盘查看、个股筛选、自选跟踪、技术指标判断和分析整理。当前手工流程分散，容易遗漏异动，也无法稳定复现某天的筛选和判断依据。

本项目建设一个个人使用的 Web 交易辅助决策工具。V1 聚焦日线行情、技术指标、股票筛选、自选股监控和规则化报告，不接实盘，不做自动下单。

### 1.2 项目目标

- 建立“同步数据 → 查看市场 → 筛选候选股 → 查看详情 → 加入自选 → 查看提醒 → 生成报告”的稳定闭环。
- 保证同一数据批次和规则版本下，详情、筛选、提醒和报告结论一致且可复现。
- 将数据异常、样本不足和规则失效条件显式展示，不用推测性文案代替数据。
- 降低每日重复整理成本，并保留可回顾的信号、报告和关注笔记。

### 1.3 成功标准

| 指标 | V1 验收标准 |
|---|---|
| 数据同步 | 连续 20 个交易日定时任务成功率不低于 95%；失败可手动重试且不产生重复数据 |
| 数据完整性 | 当前有效批次的股票日线完整率不低于 99%；缺失股票被明确标记，不进入候选结果 |
| 指标一致性 | 固定金样本上的 MA、MACD、RSI、布林带结果符合第十一章容差要求 |
| 结果一致性 | 同一批次、同一规则版本重复执行，候选股、信号和报告摘要完全一致 |
| 股票筛选 | 全市场默认筛选在目标开发机 P95 不超过 5 秒；最终阈值可在 Spike 后按实测修订 |
| 页面可用性 | 已有有效批次时，详情页和看板核心数据 P95 加载时间不超过 2 秒 |
| 提醒幂等 | 同一股票、规则和交易日最多生成一条提醒事件 |
| 可解释性 | 报告中的每条判断均可追溯到交易日、指标值、信号规则和规则版本 |

### 1.4 覆盖用户

第一版仅面向项目所有者本人，不设计注册、登录、角色和多用户数据隔离。

### 1.5 方案简述

系统通过数据源适配层获取沪、深、北交所普通 A 股的基础信息、日线行情、指数行情和交易日历，写入本地 SQLite。同步完成并校验后，统一计算指标、生成信号和候选股，再将一个完整批次切换为当前有效批次。

Web 端提供行情看板、股票详情、股票筛选、自选股监控、提醒事件和规则模板报告。所有结论仅描述技术状态、关注条件和失效条件。

### 1.6 后续方向

- 商业数据源或 Tushare 适配。
- 分钟级行情和盘中提醒。
- 策略回测、组合管理和收益跟踪。
- AI 投研摘要，但只总结已确认的数据和规则结果。
- 券商 API 仅在完成安全与合规专项评估后考虑。

## 二、名词与统一口径

| 名词 | 定义 |
|---|---|
| 交易日 T | 当前数据批次对应的最新已完成交易日 |
| 最新价/当前价 | 交易日 T 的不复权收盘价，不代表盘中实时价格 |
| 日线级分析 | 使用已完成交易日的开、高、低、收、成交量和成交额进行分析 |
| 当前有效批次 | 已完成拉取、校验、指标计算和信号生成，可供所有页面读取的数据批次 |
| 前复权行情 | 用于技术指标与信号计算的价格序列；复权因子变化后需要重算受影响区间 |
| 状态型条件 | 在交易日 T 满足条件即可命中，如 `close > MA20` |
| 事件型信号 | 必须比较 T 与 T-1 才能触发，如上穿、下穿、进入区间 |
| 候选股 | 当前有效批次中命中默认候选方案的股票 |
| 信号风险等级 | 对当前规则事件的技术风险归类，不表示股票的完整投资风险 |
| 样本不足 | 有效交易日数量不满足公式最小窗口，不计算、不补零、不外推 |
| 规则版本 | 指标参数、信号公式或阈值变化后生成的新版本标识 |

## 三、范围与优先级

### 3.1 需求概览

| 序号 | 模块 | 范围 | 优先级 |
|---|---|---|---|
| 1 | 数据采集与批次管理 | 股票池、交易日历、日线、指数、校验、失败恢复 | P0 |
| 2 | 指标与信号引擎 | 指标预计算、内置信号、风险等级、规则版本 | P0 |
| 3 | 股票详情 | K 线、指标、信号、数据口径说明 | P0 |
| 4 | 股票筛选器 | 临时组合筛选、默认候选方案、结果排序 | P0 |
| 5 | 行情看板 | 指数、市场概览、候选股、自选异动、同步状态 | P0 |
| 6 | 自选股监控 | 默认分组、添加移除、信号和提醒确认 | P0 |
| 7 | 分析报告 | 模板报告、可追溯信息、Markdown 导出 | P0 |
| 8 | 自定义提醒规则 | 规则增删改、参数设置、启停和版本化 | P1 |
| 9 | 保存筛选方案 | 新建、重命名、覆盖、删除和设为默认 | P1 |
| 10 | 关注笔记 | 按股票和交易日记录、修改、删除关注结论 | P1 |
| 11 | 系统设置 | 定时同步、默认排除项、参数查看 | P1 |

### 3.2 分阶段交付

| 里程碑 | 范围 | 依赖 |
|---|---|---|
| 阶段 A | 数据同步、批次管理、指标引擎、股票详情 | 数据接口 Spike 完成 |
| 阶段 B | 筛选器、默认候选方案、行情看板 | 阶段 A 指标数据稳定 |
| 阶段 C | 自选股、内置提醒、确认状态 | 阶段 B 信号引擎稳定 |
| 阶段 D | 报告生成、Markdown 导出 | 阶段 C 规则结果稳定 |
| P1 扩展 | 自定义规则、保存筛选方案、关注笔记、系统参数 | P0 主链路验收通过 |

## 四、核心业务流程

### 4.1 每日数据处理

```text
等待收盘后调度/用户手动触发
  → 创建同步任务
  → 拉取交易日历、股票基础信息、日线和指数
  → 标准化字段与单位
  → 完整性和合理性校验
  → 预计算指标
  → 生成状态型条件和事件型信号
  → 生成默认候选结果和提醒事件
  → 整批校验通过
  → 原子切换为当前有效批次
```

任一关键阶段失败，本次任务标记失败，不切换当前有效批次，页面继续使用上一次成功批次并展示失败原因。

### 4.2 用户研究流程

```text
查看行情看板
  → 查看数据交易日和同步状态
  → 浏览默认候选股/自选异动
  → 调整筛选条件
  → 查看股票详情和指标证据
  → 加入自选或确认提醒
  → 生成并导出分析报告
  →（P1）记录关注笔记
```

## 五、数据契约

### 5.1 股票范围

- 支持沪、深、北交所处于上市状态的普通 A 股。
- V1 排除 B 股、基金、债券、可转债、指数成分实体和 CDR。
- ST 股票默认排除，但仍允许在详情页按代码查询。
- 停牌股票保留历史数据和详情，只是不进入当日候选、不生成基于当日行情的趋势结论。
- 上市不足 120 个有效交易日的股票默认不进入候选；详情页按已有样本展示可计算指标。

### 5.2 来源映射

具体 AkShare 接口可能随版本变化，系统不得在业务层直接依赖接口字段。开发前 Spike 需锁定下表的接口、字段映射和样本结果，业务层只读取标准模型。

| 标准数据 | 首选来源类型 | 必需字段 | 降级策略 |
|---|---|---|---|
| 股票基础信息 | 沪深京 A 股代码与个股信息接口 | 代码、名称、市场、行业、上市日期、上市状态 | 行业缺失显示“未知”；代码、市场或上市状态缺失则不入股票池 |
| 个股日线 | `stock_zh_a_hist` 同类历史行情接口 | 日期、开高低收、成交量、成交额、涨跌幅、换手率 | 单股失败独立重试；仍失败则标记缺失，不覆盖旧数据 |
| 指数日线 | A 股指数历史行情接口 | 指数代码、日期、开高低收、涨跌幅 | 单指数失败时卡片显示旧数据和数据日期 |
| 交易日历 | AkShare 交易日历工具接口 | 日期、是否交易日 | 获取失败时不执行自动同步，可手动重试 |
| 市场概览 | 基于当日有效股票池的日线自行聚合 | 上涨/下跌/平盘、成交额、涨跌停近似统计 | 不依赖盘中接口；数据不完整时隐藏统计并显示完整率 |

### 5.3 标准字段与单位

| 字段 | 类型 | 统一单位/规则 |
|---|---|---|
| `stock_code` | string | 六位代码，市场单独存储 |
| `trade_date` | date | Asia/Shanghai 自然日 |
| `open/high/low/close` | decimal | 元，数据库保留至少 4 位小数，页面按证券价格精度展示 |
| `volume` | integer | 股；若来源单位为手，入库前乘以 100 |
| `amount` | decimal | 元 |
| `pct_change` | decimal | 百分数值，例如 `3.20` 表示 3.20% |
| `turnover_rate` | decimal nullable | 百分数值；缺失时不推算 |
| `is_suspended` | boolean | 当日无有效成交且来源明确为停牌时为真 |
| `adjustment` | enum | `raw` 或 `qfq` |
| `fetched_at` | datetime | 带 Asia/Shanghai 时区 |

### 5.4 复权规则

- `daily_price` 保存不复权行情，用于页面价格、涨跌幅和成交展示。
- 技术指标基于前复权价格计算；成交量不复权。
- 页面比较“收盘价与 MA”时，两者使用同一前复权序列的 T 日值；页面同时标注“指标采用前复权”。
- 发现除权除息或数据源历史修订时，至少重拉并重算最近 250 个有效交易日。

### 5.5 数据质量校验

- 主键唯一：同一市场、股票、交易日、复权口径只能有一条行情。
- OHLC 合法：`high >= max(open, close)` 且 `low <= min(open, close)`，价格和成交量不得为负。
- 交易日一致：非停牌行情的 `trade_date` 必须位于交易日历。
- 批次完整率低于 99% 时不得自动切换为当前有效批次；允许用户查看失败清单并手动强制切换，但必须显示风险标识。
- 缺失值不使用 0 填充；指标和信号按“样本不足”处理。

## 六、同步任务与批次管理

### 6.1 同步策略

- 首次同步：获取最近 3 年且不少于 750 个有效交易日的数据。
- 每日自动同步：交易日 18:30（Asia/Shanghai）执行。
- 日常增量：回补最近 5 个交易日，处理数据源延迟修订。
- 指标重算：每次至少重算最近 250 个有效交易日；复权变化时扩大到受影响区间。
- 手动同步：已有任务执行中时不创建并发任务，只展示当前进度。

### 6.2 状态机

`PENDING → FETCHING → VALIDATING → CALCULATING → GENERATING_SIGNALS → READY`

任一阶段可以转为 `FAILED`。失败任务保存失败阶段、错误摘要、已完成数量和重试次数。重试复用同一任务或创建关联子任务，但写入必须幂等。

### 6.3 原子切换与幂等

- 所有派生数据记录 `batch_id`、`trade_date`、`rule_version`。
- 只有 `READY` 批次可被设置为当前有效批次。
- 当前批次切换使用单个数据库事务完成。
- 行情唯一键：`market + stock_code + trade_date + adjustment`。
- 提醒唯一键：`stock_code + trade_date + rule_code + rule_version`。
- 重新同步不得清除提醒确认状态、关注分组或关注笔记。

## 七、指标与信号规则

### 7.1 通用规则

- 所有窗口都按有效交易日计算并包含 T 日。
- T-1 指上一有效交易日，不是上一自然日。
- 本章公式中的 `close`、`high` 默认指前复权价格；只有明确标注“不复权”的规则使用原始行情。
- 中间计算保留至少 8 位小数，落库保留至少 6 位，页面展示按指标配置舍入。
- 事件型信号只在跨越或进入当日产生一次；持续满足条件不会每日重复产生事件。
- 参数或公式变化必须提升 `rule_version`，历史事件继续保留原版本。

### 7.2 指标公式

| 指标 | V1 公式 |
|---|---|
| MA5/10/20/60 | 前复权收盘价的简单移动平均 |
| EMA | `EMA_t = α × close_t + (1-α) × EMA_(t-1)`，`α = 2/(N+1)`；首个 EMA 使用首个有效收盘价初始化 |
| MACD | `DIF = EMA12 - EMA26`；`DEA = DIF 的 EMA9`；`HIST = 2 × (DIF - DEA)` |
| RSI14 | Wilder RSI；首值使用前 14 个涨跌的简单平均，之后采用 Wilder 平滑 |
| 布林带 | 中轨为 MA20；上下轨为 `MA20 ± 2 × σ20`，`σ` 使用总体标准差 `ddof=0` |
| 量比 | `volume_ratio_5_20 = AVG(volume, 5) / AVG(volume, 20)` |
| 前高压力位 | T 日之前 60 个有效交易日的前复权最高价最大值；不足 60 日不计算 |

### 7.3 内置信号

| 规则代码 | 类型 | 触发条件 |
|---|---|---|
| `PRICE_ABOVE_MA20` | 状态 | `close_qfq[T] > MA20[T]` |
| `MA5_ABOVE_MA20` | 状态 | `MA5[T] > MA20[T]` |
| `VOLUME_EXPANDED` | 状态 | `volume_ratio_5_20[T] >= 1.20` |
| `BREAKOUT_MA20_WITH_VOLUME` | 事件 | `close[T-1] <= MA20[T-1]` 且 `close[T] > MA20[T]` 且量比不低于 1.20 |
| `FALL_BELOW_MA20` | 事件 | `close[T-1] >= MA20[T-1]` 且 `close[T] < MA20[T]` |
| `MACD_GOLDEN_CROSS` | 事件 | `DIF[T-1] <= DEA[T-1]` 且 `DIF[T] > DEA[T]` |
| `MACD_DEATH_CROSS` | 事件 | `DIF[T-1] >= DEA[T-1]` 且 `DIF[T] < DEA[T]` |
| `MACD_RED_EXPANDING` | 状态 | `HIST[T] > HIST[T-1] > 0` |
| `RSI_STRONG` | 状态 | `50 <= RSI14[T] <= 75` |
| `RSI_ENTER_STRONG` | 事件 | T-1 不在 `[50,75]` 且 T 进入 `[50,75]` |
| `RSI_OVERHEATED` | 状态 | `RSI14[T] > 80` |
| `RSI_ENTER_OVERHEATED` | 事件 | `RSI14[T-1] <= 80` 且 `RSI14[T] > 80` |
| `DAILY_SURGE` | 事件 | 不复权日涨跌幅 `>= 5%` |
| `DAILY_DROP` | 事件 | 不复权日涨跌幅 `<= -5%` |
| `NEAR_60D_HIGH` | 状态 | 前高可计算且 `0 <= (high_60_prev-close)/high_60_prev <= 3%` |

### 7.4 趋势摘要

- 短期偏强：`close > MA20`、`MA5 > MA20`、`DIF > DEA` 三项全部满足。
- 短期偏弱：`close < MA20`、`MA5 < MA20`、`DIF < DEA` 三项全部满足。
- 其他情况：震荡/方向不明确。
- 样本不足：任一必需指标不可用，不输出强弱结论。

### 7.5 信号风险等级

该等级仅描述当前技术信号，不表示完整投资风险。

- 高：命中 `FALL_BELOW_MA20`、`MACD_DEATH_CROSS` 或 `DAILY_DROP` 任一事件。
- 中：未命中高风险事件，但命中 `RSI_OVERHEATED` 或关键数据完整率不足。
- 低：数据完整，且未命中上述高、中风险条件。
- 同时存在正负信号时，风险等级按最高级展示，报告必须同时列出冲突信号。

## 八、详细产品方案

### 8.1 行情看板

展示当前有效交易日、最近同步时间、批次状态、上证指数、深证成指、创业板指、北证 50、市场概览、候选股前 20 名和自选股未确认异动。

规则：

- 页面统一使用“交易日 T 候选股”“交易日 T 收盘价”，避免“实时”“今日当前价”等歧义。
- 市场概览基于有效股票池自行聚合；涨停/跌停在 V1 仅按涨跌幅阈值近似统计，并标注“近似”。精确涨跌停规则延后。
- 无有效批次时只展示同步引导；同步失败时继续展示上次成功批次和失败摘要。
- 候选股默认最多 20 条，可进入筛选器查看完整结果。

### 8.2 默认候选方案

股票需同时满足：非 ST、非停牌、上市不少于 120 个有效交易日、`close > MA20`、`MA5 > MA20`、`45 <= RSI <= 75`，并且最近 3 个有效交易日内命中 `BREAKOUT_MA20_WITH_VOLUME` 或 `MACD_GOLDEN_CROSS`。

排序顺序：正向事件数量降序 → 量比降序 → 涨跌幅降序 → 股票代码升序。最多保存 50 条候选结果。

### 8.3 股票详情

页面包含股票名称、代码、市场、行业、交易日 T、不复权收盘价、涨跌幅、成交额、换手率、日 K、MA5/10/20/60、成交量、MACD、RSI、布林带、趋势摘要、信号风险等级和最近 10 条事件。

交互与边界：

- K 线默认展示最近 250 个有效交易日，可切换 60/120/250 日。
- 明确标注“价格展示为不复权，指标计算采用前复权”。
- 样本不足的指标显示原因和所需最小样本，不隐藏整个指标区。
- 停牌股票显示停牌状态，不生成交易日 T 的新趋势结论。
- 可加入/移出自选并选择分组；重复添加不新增记录。

### 8.4 股票筛选器

支持市场、涨跌幅区间、量比阈值、收盘价与 MA20、MA5 与 MA20、MACD 状态/事件、RSI 区间、ST、停牌和上市天数条件。所有已选条件使用 AND；同一字段的多选枚举使用 OR。

- 至少选择一个条件才能执行。
- 结果默认最多返回 200 条，支持分页和稳定排序。
- 结果显示实际使用的 `trade_date`、`batch_id` 和规则版本。
- 取消操作只取消当前查询，不修改已经计算好的指标和信号。
- 保存筛选方案为 P1；P0 只保留当前页面会话条件。

### 8.5 自选股监控

V1 内置“短线关注”“中线观察”“风险观察”三个分组和“全部”虚拟分组。一个股票在 V1 只能属于一个实际分组。

- 自选列表展示交易日 T 收盘价、涨跌幅、主要信号、信号风险等级和提醒状态。
- 提醒状态为 `UNTRIGGERED / TRIGGERED / CONFIRMED`。
- 已确认事件不再在看板高亮，但仍保留在历史事件中。
- 新数据批次产生的新事件与历史确认状态相互独立。

### 8.6 提醒规则

P0 仅启用第 7.3 节定义的内置事件型信号。页面内提醒和看板异动不发送短信、微信、邮件或系统通知。

P1 才支持自定义阈值、启停和新建规则。规则修改生成新版本，不回写历史提醒；旧版本事件仍可查看。

### 8.7 分析报告

报告按股票、交易日、数据批次和规则版本生成，结构如下：

1. 数据口径与完整性
2. 趋势判断
3. 技术指标
4. 量能变化
5. 关注理由
6. 风险与冲突信号
7. 条件触发与失效条件
8. 结论摘要
9. 免责声明

报告规则：

- 只使用已落库的指标与信号，不在报告模块重新计算。
- 每条结论包含触发规则和输入值，可展开查看。
- 相同股票、交易日、批次和模板版本重新生成时创建新报告版本，不覆盖旧报告。
- V1 仅支持 Markdown 导出，文件名为 `{trade_date}-{stock_code}-{report_version}.md`。
- 固定显示：“本工具仅用于个人研究和信息整理，不构成投资建议。历史数据和技术指标不代表未来表现。”
- 禁止出现“买入、卖出、必涨、目标价、收益保证”等直接建议或承诺。

### 8.8 系统设置（P1）

- 查看数据源适配器版本、最近成功批次、失败任务和数据完整率。
- 设置自动同步启停和执行时间，默认交易日 18:30。
- 查看指标参数和当前规则版本。
- 修改指标参数时创建新规则版本并触发明确的重算确认，不静默修改历史结果。

### 8.9 关注笔记（P1）

- 用户可按股票和交易日创建一条关注笔记，记录关注理由、观察条件和失效条件。
- 笔记支持修改和可恢复删除，并保留创建、更新时间。
- 笔记不参与信号、风险等级和报告自动结论计算。

## 九、数据模型

| 表 | 关键字段 | 关键约束 |
|---|---|---|
| `stock_basic` | market、stock_code、name、industry、list_date、status、is_st | `market + stock_code` 唯一 |
| `trade_calendar` | trade_date、is_open、source | trade_date 唯一 |
| `sync_job` | id、target_trade_date、status、stage、progress、error_summary、started_at、finished_at | 保存任务状态和重试关系 |
| `data_batch` | id、trade_date、status、completeness_rate、rule_version、activated_at | 同一时刻只能有一个当前有效批次 |
| `daily_price` | batch_id、market、stock_code、trade_date、adjustment、OHLCV、amount、pct_change、turnover_rate | 行情复合唯一键 |
| `index_daily` | batch_id、index_code、trade_date、OHLC、pct_change | `batch_id + index_code + trade_date` 唯一 |
| `daily_indicator` | batch_id、stock_code、trade_date、MA、MACD、RSI、BOLL、volume_ratio、high_60_prev | `batch_id + stock_code + trade_date + rule_version` 唯一 |
| `signal_event` | batch_id、stock_code、trade_date、rule_code、rule_version、payload | 提醒唯一键 |
| `candidate_result` | batch_id、stock_code、rank、matched_rules、score_snapshot | `batch_id + stock_code` 唯一 |
| `watchlist_group` | id、name、sort_order | name 唯一 |
| `watchlist_item` | stock_code、group_id、created_at | stock_code 唯一 |
| `alert_event_state` | signal_event_id、status、confirmed_at | signal_event_id 唯一 |
| `analysis_report` | id、stock_code、trade_date、batch_id、rule_version、template_version、report_version、content | 同一报告上下文按版本唯一 |
| `screener_preset` | id、name、conditions_json、is_default | P1；name 唯一 |
| `decision_note` | id、stock_code、trade_date、content、created_at、updated_at、deleted_at | P1；默认按股票和交易日一条有效记录 |

## 十、非功能需求

### 10.1 性能与容量

- 目标股票池按 6,000 只、每只 3 年日线设计容量。
- 筛选查询只访问标准化行情、缓存指标和信号，不在请求过程中全量重算。
- 看板、详情和筛选均须使用分页、索引或预聚合，避免一次返回全部历史数据。
- 首次同步和全量重算允许后台运行，页面展示阶段、完成数、失败数和预计剩余量。

### 10.2 可靠性

- 所有同步写入和派生计算支持幂等重试。
- 应用异常退出后，未完成任务可识别为中断并重新执行。
- 每日自动备份 SQLite 数据库，默认保留最近 7 份；恢复流程在技术设计中定义并实测。
- 日志不得记录完整报告正文、用户笔记或潜在密钥。

### 10.3 兼容性

- 支持最新版 Chrome 和 Edge 桌面浏览器。
- V1 不承诺移动端布局和 Safari 专项兼容。
- 页面时区固定为 Asia/Shanghai。

### 10.4 安全与合规

- 服务默认只监听 `127.0.0.1`，不得默认监听 `0.0.0.0`。
- 若后续开放局域网或公网访问，必须先增加认证、HTTPS、CSRF 防护、访问日志和配置密钥管理，本 PRD 不授权直接开放。
- 数据源配置和潜在凭据只保存在服务端环境配置，不进入前端、日志、报告或代码仓库。
- 首页、报告页和导出文件固定展示投资免责声明。

## 十一、验收标准

### 11.1 数据与同步

- 首次同步、每日增量、手动重试均可完成，并形成可查看的 `sync_job` 和 `data_batch`。
- 模拟单只股票请求失败时，任务记录失败清单，不用空值覆盖该股票旧数据。
- 在校验或计算阶段主动制造失败，当前有效批次保持不变。
- 对同一交易日连续同步两次，行情、指标和事件不重复。
- 非交易日不自动创建日线批次，手动同步给出明确提示。

### 11.2 指标金样本

准备不少于 100 只股票、250 个有效交易日的固定样本，覆盖沪深京、ST、停牌、次新、除权、连续涨跌和成交量为零等情形。

- MA、MACD、RSI、布林带与选定基准实现逐点比对。
- MA/MACD/RSI 绝对误差不超过 `1e-6`；布林带绝对误差不超过 `1e-6`。
- 由于初始化方式导致的早期差异，必须在测试说明中固定预热区间，不得通过放宽全局容差掩盖。
- 样本不足时返回空值和原因，不返回 0。

### 11.3 筛选、提醒与报告

- 相同批次和规则版本重复筛选，结果及排序完全一致。
- `BREAKOUT_MA20_WITH_VOLUME` 连续满足三天时，只在实际上穿日产生事件。
- 同一提醒重复同步不新增事件，确认状态不被重置。
- 报告中的趋势、指标、信号与详情页完全一致。
- 报告重新生成产生新版本，旧版本可查看。
- Markdown 导出包含交易日、批次、规则版本、数据完整性和免责声明。

### 11.4 页面异常状态

- 无数据、同步中、同步失败、数据过期、样本不足、停牌和部分字段缺失均有独立状态。
- 页面展示上次有效交易日，不能把旧数据标成实时或今日数据。
- 无候选股时展示空状态，不自动放宽条件。

## 十二、效果追踪

个人本地 V1 使用结构化操作日志，不建设独立埋点平台。

| 指标 | 计算方式 |
|---|---|
| 同步成功率 | READY 任务数 / 已结束任务数 |
| 数据完整率 | 有有效日线的目标股票数 / 目标股票池数量 |
| 筛选使用次数 | `screener_search` 操作日志计数 |
| 报告生成次数 | `analysis_report` 新版本数量 |
| 提醒确认率 | 已确认提醒数 / 已触发提醒数 |
| 自选活跃度 | 近 30 日进入自选详情的股票数 |

日志只保留事件名、时间、页面、批次和必要标识，不记录完整笔记或报告正文。

## 十三、开工前数据 Spike

开发正式功能前用 1～2 天完成以下验证，并将结果写入技术设计：

1. 选择 20 只覆盖沪深京、ST、停牌、次新、除权和 CDR 边界的样本证券。
2. 锁定股票池、基础信息、日线、指数和交易日历的 AkShare 具体接口及字段映射。
3. 验证成交量、成交额、换手率、涨跌幅和复权字段的单位与缺失行为。
4. 计算 250 个交易日的全部指标，与基准实现比对。
5. 扩大到全市场，实测首次同步、增量同步、指标预计算和组合筛选耗时。
6. 根据实测结果确认第 1.3 节性能阈值；若无法达标，先调整方案再进入页面开发。

## 十四、V1 不做范围

| 内容 | 原因 |
|---|---|
| 自动交易和券商 API | 风险高，超出辅助研究范围 |
| 分钟级和盘中实时行情 | V1 只处理已完成日线 |
| AI 预测股价和强买卖建议 | 不可解释且存在误导风险 |
| 策略回测 | 当前成功标准不包含 |
| 多用户和权限系统 | 当前仅个人本机使用 |
| 手机 App 和移动端专项适配 | 第一版先做桌面 Web |
| 精确涨跌停规则引擎 | 涉及板块、ST、上市阶段等复杂规则，V1 仅做近似市场统计 |
| PDF 导出 | V1 只支持 Markdown |

## 十五、评审记录

| 日期 | 结论 | 处理结果 |
|---|---|---|
| 2026-08-25 | 原 V1 方向可行，但数据契约、信号口径、同步时序和验收标准不足 | 已在 V1.1 补充；具体 AkShare 接口仍须以开工前 Spike 实测锁定 |
