# T007 前端 P0 证据

## 红灯

- 新页面测试首次全部失败，证明旧前端仍是静态空状态。
- 页面测试转绿后，首次覆盖率检查因 functions 43%、branches 68% 被 70% 门槛拒绝。
- 类型检查发现 ECharts 实例类型不完整，随后修正并改为模块化按需加载。

## 绿灯

- `openapi-typescript` 从后端快照生成类型，前端请求层不复制响应结构。
- 看板区分 loading、无批次、失败、历史数据和成功状态，不把旧数据标成实时。
- 筛选、自选增删、提醒确认、报告生成导出和股票详情路由可操作。
- 股票详情按需加载 ECharts，主包与图表包分离且构建无大包警告。
- 8 个页面与交互测试通过；statements 91%、branches 76%、functions 80%。
- TypeScript 类型检查与 Vite production build 通过。

## 未验证

- 尚未在真实浏览器做窄屏、键盘、控制台和视觉验收。
- 指数卡片、完整 K 线/副图与真实进程数据将在闭环审计中补齐。
