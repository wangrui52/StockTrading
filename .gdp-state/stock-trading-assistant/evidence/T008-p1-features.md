# T008 P1 功能证据

## 红灯

- 五组 P1 API 契约首次全部返回 404。
- 新前端功能进入后继续由覆盖率、类型检查和 build 门槛验证。

## 绿灯

- 筛选方案名称唯一，默认方案切换不会静默保留多个默认值。
- 关注笔记支持创建、修改、软删除、查看历史和恢复。
- 指标参数修改必须显式确认重算，并创建 `v2`；历史 `v1` 信号未被改写。
- 自定义提醒规则修改创建新版本，历史阈值可查询。
- 自动同步默认 18:30，可修改启停与时间。
- 前端提供筛选方案保存、详情关注笔记、同步设置、规则重算确认和自定义提醒入口。
- 后端默认集 44 passed、1 skipped，覆盖率 94%；前端 9 passed，覆盖率三项均不低于 70%；两端构建检查通过。

## 未验证

- 自定义提醒规则尚未在全市场固定样本中回放。
- 笔记恢复冲突与多进程并发仍需 E2E/部署数据库复核。
