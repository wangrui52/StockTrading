# T009 本地 E2E 与 PRD 审计证据

## 功能闭环

- 固定 100 只股票、250 个交易日样本完成同步、看板、详情、筛选、自选、提醒、报告与 Markdown 导出闭环。
- 股票详情包含 60/120/250 日 K 线、MA/BOLL、成交量、MACD、RSI、趋势、风险和事件。
- P1 筛选方案、可恢复笔记、规则版本、自定义提醒和定时设置均有生产实现。
- 增量同步回补最近约 5 个交易日；检测前复权历史修订后扩大回拉到至少 250 个交易日。
- 筛选和自选详情访问写入结构化操作日志，不记录笔记或报告正文。

## 自动化验证

- 后端：61 passed、1 skipped（显式真实网络测试），覆盖率 91.67%，Ruff 通过。
- 前端：12 passed；statements 92.16%、branches 79.16%、functions 74.21%、lines 92.16%；typecheck 和 production build 通过。
- Playwright：桌面 Chromium 与 Pixel 7 两项目 2 passed，控制台错误断言为空。
- Alembic：空 SQLite 从 base 升级到 `a9f3c8d2e710` 通过。
- 固定 100×250 样本筛选 30 次的 P95 为 9.56ms；此结果不代表 6,000 只全市场性能。

## 未验证边界

- 未执行 6,000 只真实股票的首次同步、限流和吞吐压测。
- 最新一轮默认测试未访问 AkShare；此前单股 `600000` live 小样本通过。
- 连续 20 个交易日定时成功率属于上线后观测指标，当前不能用一次性测试替代。
