# Handoff

## 当前目标
按 PRD V1.1 实现完整前后端、测试和部署基础。

## 当前 task
T006 P0 查询与命令 REST 接口。

## 最近证据
T005 同步成功、部分失败保护、同日幂等、非交易日保护和 AkShare 冻结/真实响应测试通过；默认集 33 passed、覆盖率 94%，live 小样本 1 passed。

## 未完成事项
全部生产实现、测试和运行验证。

## 下一步建议
先以 API 契约测试锁定状态、同步、看板、详情、筛选、自选、提醒和报告端点，再实现 application services。

## 已知风险
真实 AkShare 单股小样本已通过，但全市场吞吐、限流和字段漂移仍需上线前压测；SQLite 不代表 PostgreSQL 并发行为。
